<h1><%= t("login") %></h1>

<%= form_with(url: login_path, local: true) do |form| %>
  <%= form.label :email %>
  <%= form.text_field :email %>
  <%= form.label :password %>
  <%= form.password_field :password %>

  <div class="actions">
    <%= form.submit t("login") %>
  </div>
<% end %>
